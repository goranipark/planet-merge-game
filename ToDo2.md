# ver2 — 거리 순서 모드 ToDo

[concept2.md](concept2.md) 11장을 실행 체크리스트로 옮긴 문서입니다.
ver1 체크리스트는 [ToDo.md](ToDo.md)에 있습니다.

각 단계는 **끝나면 화면에서 눈으로 확인할 수 있는 단위**로 끊었습니다.
한 단계가 끝날 때마다 실행 방법을 안내하고, 확인이 끝난 뒤 다음 단계로 넘어갑니다.

> ⚠️ **ver2가 완성될 때까지 배포하지 않습니다.**
> 작업은 `ver2` 브랜치에서만 하고, `main`은 학생들이 쓰는 배포본 그대로 둡니다.
> (`main`에 push하면 GitHub Actions가 자동 배포하기 때문입니다)
> 검증은 로컬 개발 서버(`npm run dev`)로만 하고, 실제 웹 테스트는 완성 후에 합니다.

---

## 진행 상황

- [x] **1. 모드 구조 만들기** — `modes/` 도입, ver1을 `sizeMode`로 감싸기
- [x] **2. 모드 선택 화면** — 첫 화면에서 모드 고르기, 거리 모드는 "공사 중"으로 잠금
- [x] **3. 거리 모드 데이터** — 8행성 데이터·궤도 반지름·점수표 입력
- [x] **4. 궤도 고리 스프라이트** — 고리 + 얼굴 + 행성 점 SVG
- [x] **5. 거리 모드 플레이 가능** — 엔진 연결, 순서표 패널 교체, **공사 중 잠금 해제**
- [x] **6. 순서 퀴즈 카드** — 3지선다, 보너스 점수, `ORDER_QUIZ` 토글
- [x] **7. 엔딩 거리자** — 실제 비율 배치, 확대 토글, 명왕성 카드
- [x] **8. 순위표 모드 분리** — `gameMode` 필드·색인·보안 규칙 (배포는 12번에서)
- [x] **9. 밸런싱** — 라벨 가독성 수정, 해왕성 중복 처리, 난이도는 그대로
- [x] **10. 문서 갱신** — 배포-안내, 안내문-모음, README
- [ ] **11. 학생 파일럿** — "목성 vs 해왕성" 확인 질문 포함
- [x] **12. 배포** — 완료 (2026-09-08). 두 게임 모두 열려 있음

> concept2.md 11장은 모드 선택 화면을 7번에 두었는데, 실제로는 2번으로 앞당겼습니다.
> 이후 단계를 실제 화면 안에서 만들 수 있고, 완성 전까지 거리 모드가 눌리는 사고도 막히기 때문입니다.

---

## 단계별 상세

### 1. 모드 구조 만들기 ✅

겉으로 보이는 변화가 **없는** 단계입니다. ver1이 이전과 똑같이 동작하면 성공입니다.

- `src/game/modes/sizeMode.jsx` — ver1 데이터를 모드 객체로 감싸기 (안내 문구에 굵은 글씨가 있어 .jsx)
- `src/game/modes/index.js` — 모드 목록과 조회 함수
- `engine.js` — `createGame(el, { mode, ... })`로 모드를 주입받도록 변경
- `mergeLogic.js` — 모드의 단계 수·점수표를 쓰도록 변경
- `config.js` — 모드별 설정(`MODE_TUNING`)으로 정리
- `PlanetGuide` / `NextPreview` / `InfoCard` — 모드를 받아 쓰도록 변경

**확인 방법**: 게임을 한 판 해서 낙하·병합·점수·정보 카드·순서표가 모두 이전과 같은지 본다.

### 2. 모드 선택 화면 ✅

- `src/game/modes/distanceMode.jsx` — 거리 모드 **껍데기**. `comingSoon: true`로 잠겨 있음
- `src/components/ModeSelect.jsx` — 두 모드를 카드로 보여주고 고르기
- `src/game/modeStorage.js` — 고른 모드 기억(별명과 같은 규칙: 탭 닫으면 초기화), `?mode=` 링크 지원
- 제목 아래 **현재 모드 표시 겸 "바꾸기" 버튼**
- 공사 중인 모드는 노랑-검정 공사장 테이프로 막고 눌러도 시작되지 않음
  (모드 이름은 테이프 위에 남겨 두어 "무엇이 준비 중인지"는 읽히게 함)

**화면 순서**: 모드 선택 → 별명 고르기 → 게임

> `?mode=distance` 링크로 들어와도 잠금은 풀리지 않고 선택 화면이 뜹니다.
> **잠금을 푸는 곳은 `distanceMode.jsx`의 `comingSoon` 한 줄**이며, 5번 단계에서 지웁니다.

### 3. 거리 모드 데이터 ✅

- `src/game/orbits.js` — 8행성 데이터 (거리, AU, 공전 주기, 지름, 설명, 상식)
- 궤도 반지름은 concept2.md 8장 **(C) 절충안** 채택, (A)(B) 값도 주석으로 남김
- `MODE_TUNING.distance` — `spawnPoolSize: 3`, `mergeScores: [0,10,25,45,75,120,190,300]`
- `scripts/check-orbits.mjs` — 데이터가 규칙에 맞는지 검사하는 스크립트

**확인 방법**: `npm run check:orbits` — 표가 출력되고 "모두 통과했습니다"가 나오면 성공.
숫자를 손본 뒤에도 이 명령으로 게임이 깨지는 값인지 바로 확인할 수 있습니다.

### 4. 궤도 고리 스프라이트 ✅

- `src/game/orbitSprites.js` — 고리 + 안쪽 우주(얼굴) + 고리 위 행성 점
- 행성 점 크기는 **실제 지름 비율**을 압축해 매핑 (목성이 항상 가장 크게)
- ver1의 표정 6종을 그대로 재사용. 어두운 우주 위에 그리므로 선 색만 밝게 바꿔 칠함
- 고리 굵기·점 크기는 **화면 픽셀 기준**으로 지정 (단계마다 축소 배율이 다르므로 미리 보정)
- 고리 안쪽은 어두운 우주 + 얼굴로 채움 → concept2.md 열린 질문 1 해결

**순번·이름 라벨은 5번 단계로 미뤘습니다.** 그림 안에 글자를 넣으면 공이 구를 때
글자도 함께 돌아가 읽을 수 없기 때문에, 엔진이 화면 위에 따로 그리는 방식으로 만듭니다.

**확인 방법**: `npm run check:orbits` — 고리가 모든 단계에서 화면 4px로 나오는지,
행성 점이 표의 값과 일치하는지 자동으로 검사합니다.

### 5. 거리 모드 플레이 가능 ✅

- `distanceMode.jsx`의 `comingSoon` 제거 → 잠금 해제, 실제로 플레이 가능
- 공 위에 **순번 + 이름 라벨**을 엔진이 화면 기준으로 그림 (4번에서 미뤄 온 작업)
  떨어뜨리기 전 조준 미리보기에도 같은 이름표가 붙음
- `InfoCard` 를 모드가 정하는 대로 그리도록 변경
  - 크기 순서 게임 → 실제 지름 / 지구와의 크기 비교 (이전과 동일)
  - 거리 순서 게임 → 태양까지 거리 / 지구와의 거리 비교
  - `sizeNote` 가 있는 천체(천왕성·해왕성)에는 ⚠️ 오개념 주의 문구를 함께 표시
- 마지막 단계를 만들면 **완성 배너** 표시 (엔딩 화면은 7번에서)
  크기 순서 게임에서 태양을 만들 때도 함께 뜹니다 (전에는 아무 표시가 없었음)

**확인 방법**: `?mode=distance` 로 들어가거나 첫 화면에서 "거리 순서 게임"을 고른 뒤 한 판.

### 6. 순서 퀴즈 카드 ✅

- `src/components/OrderQuiz.jsx` — 3지선다. 정보 카드 **안에서** 먼저 묻고,
  답하면 그 자리에서 카드 내용이 열립니다 (창이 두 번 뜨지 않음)
- 보기는 **정답 + 순서상 가장 가까운 행성 2개**. 멀리 떨어진 행성을 주면 너무 쉬워서
  이웃한 행성끼리 헷갈리게 두었습니다 (concept2.md 열린 질문 3 해결)
- 정답 시 보너스 점수(해당 단계 점수의 50%) + 밝은 칭찬음
- 틀려도 벌점 없이 "화성 다음은 목성이에요. 수-금-지-화-목-토-천-해!"처럼 알려주고 넘어감
- 한 번 본 궤도는 다시 묻지 않음 (ver1 `seenStages` 그대로 재사용)
- `config.js`의 `ORDER_QUIZ`로 켜고 끄기. 크기 순서 게임에는 나오지 않음

**확인 방법**: 거리 순서 게임에서 새 궤도를 만들면 카드 대신 질문이 먼저 나옵니다.

### 7. 엔딩 거리자 ✅

- `src/components/DistanceRuler.jsx` — 태양~해왕성을 **실제 비율 그대로** 배치
- 전체 보기에서는 수·금·지·화가 왼쪽 끝에 뭉쳐서 이름표가 겹칩니다.
  "몰려 있다"가 요점이므로 점은 그대로 두되 이름은 **하나로 묶어** 표시하고,
  하나씩 보려면 **"안쪽 네 행성 확대"** 탭을 쓰게 했습니다
- 빛 도달 시간(태양→지구 8분 19초 / 태양→해왕성 4시간 10분)은 거리에서 계산
- "명왕성은 왜 빠졌을까요?" 카드
- **여는 방법 두 가지**
  1. 해왕성 궤도를 완성하면 자동으로 뜸 (게임은 잠시 멈춤)
  2. 거리 순서표 아래 **🔭 실제 거리 보기** 버튼 — 수업 정리 때 언제든
- 크기 순서 게임에는 나오지 않음 (`hasDistanceRuler`)

**확인 방법**: 거리 순서 게임에서 순서표 아래 "🔭 실제 거리 보기"를 누릅니다.

### 8. 순위표 모드 분리 ✅ (코드만 완료 — 배포는 12번에서)

두 게임은 점수 체계가 달라서(크기 순서 11단계 / 거리 순서 8단계) 순위표를 섞으면 안 됩니다.

- 기록에 **`gameMode`** 필드 추가 (`size` / `distance`)
  → `leaderboard.js` 안의 `mode` 는 이미 "온라인/로컬"을 뜻해서 이름을 나누어 썼습니다
- `leaderboard.js` — 제출·조회·오프라인 재전송 큐 모두 모드별로 분리.
  예전에 쌓인 기록은 `size`로 보정
- `LeaderboardPanel` — 순위표 위에 **"🛰️ 거리 순서 게임 기록"** 표시.
  모드를 바꾸면 새로고침 없이 그 모드 순위로 바뀜
- `firestore.rules` — `gameMode` 허용. **없어도 통과**시킵니다.
  규칙을 올리는 순간 게임을 켜 두고 있던 학생의 기록이 거부되지 않게 하려는 것입니다
- `firestore.indexes.json` — 색인 3개를 `(room, gameMode, 기간키, score desc)`로 변경

**확인 방법**: 모드를 바꿔 가며 순위표에 서로 다른 기록이 뜨는지 봅니다.

> ⚠️ **배포하지 않았습니다.** 규칙·색인을 지금 올리면 학생들이 쓰는 배포본에 영향이 갑니다.
> 실제 반영은 12번 단계에서 순서를 지켜 진행합니다.
>
> **지금 로컬에서 실행하면 순위표가 "아직 기록이 없어요"로 보이는 것이 정상입니다.**
> 새 색인이 아직 서버에 없어서 조회가 실패하고, 이 기기 기록으로 대체되기 때문입니다.

### 9. 밸런싱 ✅

**1) 이름표가 작은 화면에서 안 읽히던 문제 (열린 질문 2) — 고침**

글자 크기를 물리 좌표 기준으로 정하고 있어서, 병이 축소되면 글자도 같이 줄었습니다.
이제 **화면에 찍히는 크기(10~14px)를 먼저 정하고 축소 비율만큼 되돌려** 그립니다.

| 화면 | 병 폭 | 가장 작은 궤도 이름표 (전 → 후) |
|---|---|---|
| 크롬북 1366×768 | 369px | 8.3px → **10px** |
| 세로 짧은 화면 1024×460 | 234px | 5.3px → **10px** |

**2) 해왕성 궤도가 쌓여 병을 막는 문제 (열린 질문 4) — 고침**

더 합칠 데가 없는 가장 큰 궤도가 바닥에 쌓이면 게임이 금방 끝납니다.
**해왕성 궤도 두 개가 만나면 "태양계 하나 완성"으로 사라지며 +500점**을 줍니다.
완성한 뒤에도 게임을 계속할 수 있습니다. (`config.js`의 `finalPairScore`)
크기 순서 게임은 이전과 같습니다 — 태양 둘은 그대로 남습니다.

**3) 궤도 반지름·점수표·쿨다운 — 그대로 두기로 함**

자동 플레이로 두 모드를 같은 조건에서 재 봤습니다(무작위 위치로 190번 낙하).

| | 점수 | 도달 단계 | 게임오버 |
|---|---|---|---|
| 크기 순서 게임 | 2,510 | 8 / 11단계 | 0회 |
| 거리 순서 게임 | 2,383 | 7 / 8단계 | 0회 |

**점수가 오르는 속도가 거의 같아** 손댈 이유가 없다고 판단했습니다.
거리 순서 게임이 끝까지 가기 조금 더 쉬운데, 엔딩 거리자를 학생들이 실제로 보게 하려면
그 편이 낫다고 보고 그대로 두었습니다.

### 10. 문서 갱신 ✅

- [배포-안내.md](배포-안내.md)
  - **0장 신설** — 게임이 두 개라는 것, 비교표, `?mode=` 링크, 순위표가 나뉜다는 점
  - 3장 학생 안내에 모드별 순서표 읽는 법·순서 퀴즈·실제 거리 보기 추가
  - 4장에 점검 모드(`MAINTENANCE`) 사용법 추가
  - 5장 한계에 "두 순위표는 서로 다름", "거리 게임의 공은 궤도" 추가
  - 6장 소개글 두 편을 두 게임 기준으로 다시 씀
- [안내문-모음.md](안내문-모음.md) — 1·2·3·4·6번 안내문을 두 게임 기준으로 수정.
  6번 수업 스크립트에는 **실제 거리 보기를 띄우고 할 정리 발문**을 넣음
- `planet-merge-game/README.md` — 두 게임 비교표, 모드 구조(`modes/`), 점검 모드,
  `npm run check:orbits` 안내

**확인 방법**: 문서를 읽고 실제 화면과 맞는지 대조합니다.

### 11. 학생 파일럿

- 두 모드를 각각 해보게 한 뒤 확인 질문:
  - "수금지화목토천해를 순서대로 말해볼까요?"
  - **"목성과 해왕성 중 어느 쪽이 더 큰가요?"** (크기 오개념 확인 — concept2.md 6장)
  - "화성과 목성 사이는 왜 그렇게 멀까요?"

### 12. 배포 ✅ (2026-09-08 완료)

실제로 진행한 순서와 결과입니다.

1. ✅ **잠그기** — `MAINTENANCE = true` 인 채로 `ver2` → `main` 머지·push.
   실제 주소가 "잠시 준비 중이에요"로 바뀐 것을 확인
2. ✅ **색인 배포** — 3개(오늘·이번 주·이번 달) 모두 "사용 설정됨" 확인 (약 5분 소요)
3. ✅ **보안 규칙 배포** — 문법 검사 통과 후 반영
4. ⏭️ **순위표 초기화는 하지 않았습니다** — 아래 참고
5. ✅ **열기** — `MAINTENANCE = false` 로 바꿔 push, 두 게임 모두 열림
6. ✅ **확인** — 모드 선택 → 별명 → 거리 순서 게임 진행 → 게임오버 →
   "순위표에 등록했어요!" → 서버에 `gameMode: distance` 기록이 실제로 저장된 것까지 확인

> **순위표 초기화를 건너뛴 이유**
> 예전 기록 300건에는 `gameMode`가 없어 **새 순위표 조회에 애초에 잡히지 않습니다.**
> 즉 지우지 않아도 학생 화면에는 안 나옵니다. 되돌릴 수 없는 작업이라 남겨 두었습니다.
> 완전히 비우고 싶으시면 언제든 아래를 실행하세요.
> ```bash
> firebase firestore:delete scores --recursive --force --project planet-merge-game-gorani
> ```

> **남은 테스트 기록 한 건**: 쓰기 확인용으로 학급 코드 `qa-test`에 1건을 넣었습니다.
> 학생들이 쓰는 전체 순위표(`all`)나 학급 코드에는 보이지 않습니다.

#### 다음에 또 배포할 때 (참고용 절차)

**이 단계 전에는 `main`에 push하지 않고, Firebase에도 아무것도 올리지 않습니다.**

**그날까지 학생들은 ver1을 그대로 씁니다.** 배포하는 그 시간에만 잠깐 잠그고,
정리가 끝나면 바로 엽니다. (잠기는 시간은 색인 만들어지는 시간 포함 10~20분 정도)

> 🚧 `config.js`의 `MAINTENANCE`는 **true**로 두었습니다.
> 1번에서 이대로 배포하면 사이트가 "준비 중"으로 잠기고, 5번에서 false 로 바꿔 엽니다.
> (개발 중 게임을 확인할 때는 잠시 false 로 두었다가 꼭 되돌려 놓으세요)

1. **잠그기** — `MAINTENANCE = true` 인 채로 `ver2` → `main` 머지 후 push
   → 학생 화면이 "잠시 준비 중이에요"가 되고, 이후로는 순위표에 새 기록이 들어오지 않습니다
2. **색인 올리고 다 만들어질 때까지 기다리기** (몇 분 걸립니다)
   ```bash
   firebase deploy --only firestore:indexes --project planet-merge-game-gorani
   ```
   Firebase 콘솔 → Firestore → 색인 탭에서 3개가 모두 "사용 설정됨"이 될 때까지 대기
3. **보안 규칙 올리기**
   ```bash
   firebase deploy --only firestore:rules --project planet-merge-game-gorani
   ```
4. **순위표 초기화** — 예전 기록에는 `gameMode`가 없어 새 순위표에 보이지 않습니다.
   보안 규칙이 수정을 막고 있어 소급 입력도 불가능하므로 한 번 비우고 시작합니다.
   ```bash
   firebase firestore:delete scores --recursive --force --project planet-merge-game-gorani
   ```
   > 남겨야 할 기록이 있으면 **이 명령 전에** Firebase 콘솔에서 내려받으세요.
5. **열기** — `MAINTENANCE = false` 로 바꿔 커밋하고 `main`에 push → 자동 배포
6. 실제 주소에서 두 모드가 모두 열리는지, 순위표에 점수가 올라가는지 확인
