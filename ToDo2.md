# ver2 — 거리 순서 모드 ToDo

[concept2.md](concept2.md) 11장을 실행 체크리스트로 옮긴 문서입니다.
ver1 체크리스트는 [ToDo.md](ToDo.md)에 있습니다.

각 단계는 **끝나면 화면에서 눈으로 확인할 수 있는 단위**로 끊었습니다.
한 단계가 끝날 때마다 실행 방법을 안내하고, 확인이 끝난 뒤 다음 단계로 넘어갑니다.

---

## 진행 상황

- [x] **1. 모드 구조 만들기** — `modes/` 도입, ver1을 `sizeMode`로 감싸기
- [ ] **2. 거리 모드 데이터** — 8행성 데이터·궤도 반지름·점수표 입력
- [ ] **3. 궤도 고리 스프라이트** — 고리 + 행성 점 + 순번 라벨 SVG
- [ ] **4. 거리 모드 플레이 가능** — 엔진 연결, 순서표 패널 교체
- [ ] **5. 순서 퀴즈 카드** — 3지선다, 보너스 점수, `ORDER_QUIZ` 토글
- [ ] **6. 엔딩 거리자** — 실제 비율 배치, 확대 토글, 명왕성 카드
- [ ] **7. 모드 선택 화면** — 첫 화면 선택 + `?mode=` 파라미터
- [ ] **8. 순위표 mode 분리** — 필드·색인·보안 규칙, 순위표 초기화
- [ ] **9. 밸런싱** — 반지름·점수·쿨다운, 크롬북 라벨 가독성
- [ ] **10. 문서 갱신** — 배포-안내, 안내문-모음, README
- [ ] **11. 학생 파일럿** — "목성 vs 해왕성" 확인 질문 포함

---

## 단계별 상세

### 1. 모드 구조 만들기

겉으로 보이는 변화가 **없는** 단계입니다. ver1이 이전과 똑같이 동작하면 성공입니다.

- `src/game/modes/sizeMode.js` — ver1 데이터를 모드 객체로 감싸기
- `src/game/modes/index.js` — 모드 목록과 조회 함수
- `engine.js` — `createGame(el, { mode, ... })`로 모드를 주입받도록 변경
- `mergeLogic.js` — 모드의 단계 수·점수표를 쓰도록 변경
- `config.js` — 모드별 설정(`MODE_TUNING`)으로 정리
- `PlanetGuide` / `NextPreview` / `InfoCard` — 모드를 받아 쓰도록 변경

**확인 방법**: 게임을 한 판 해서 낙하·병합·점수·정보 카드·순서표·게임오버가 모두 이전과 같은지 본다.

### 2. 거리 모드 데이터

- `src/game/orbits.js` — 8행성 데이터 (거리, AU, 공전 주기, 지름, 설명, 상식)
- 궤도 반지름은 concept2.md 8장 **(C) 절충안** 채택, (A)(B) 값도 주석으로 남김
- `MODE_TUNING.distance` — `spawnPoolSize: 3`, `mergeScores: [0,10,25,45,75,120,190,300]`

### 3. 궤도 고리 스프라이트

- `src/game/orbitSprites.js` — 고리 + 중심 표식 + 고리 위 행성 점 + 순번/이름 라벨
- 행성 점 크기는 **실제 지름 비율**을 압축해 매핑 (목성이 항상 가장 크게)
- 8단계를 한 줄로 늘어놓은 미리보기로 눈으로 검증
- 고리 안쪽 채움 정도 확인 (concept2.md 13장 열린 질문 1)

### 4. 거리 모드 플레이 가능

- 엔진에 `distanceMode` 주입 → 처음부터 끝까지 한 판 진행
- `PlanetGuide` 문구를 거리 순서용으로 교체 (concept2.md 5-3)
- 해왕성 궤도 도달 시 클리어 처리 자리만 마련 (엔딩은 6번에서)

### 5. 순서 퀴즈 카드

- `src/components/OrderQuiz.jsx` — 3지선다, 보기 섞기, 틀려도 진행
- 정답 시 보너스 점수(해당 단계 점수의 50%) + 칭찬 효과음
- 한 번 본 궤도는 다시 묻지 않음 (ver1 `seenStages` 재사용)
- `config.js`의 `ORDER_QUIZ`로 켜고 끄기

### 6. 엔딩 거리자

- `src/components/DistanceRuler.jsx` — 실제 비율 배치
- 안쪽 4행성 확대 토글, 빛 도달 시간 표시
- "명왕성은 왜 빠졌을까?" 보너스 카드
- 수업 정리용 상시 버튼

### 7. 모드 선택 화면

- `src/components/ModeSelect.jsx` — 첫 화면에서 두 모드 소개 + 선택
- `?mode=distance` URL 파라미터, 학급 코드와 함께 사용 가능
- 선택 기억 (별명과 같은 저장 규칙: 탭을 닫으면 초기화)
- 게임 화면 상단에 모드 전환 버튼

### 8. 순위표 mode 분리 ⚠️

- 기록에 `mode` 필드 추가
- Firestore 색인 3개를 `(room, mode, 기간키, score desc)`로 변경
- 보안 규칙에 `mode` 허용값 추가
- **순위표 초기화 필요** — 기존 기록에는 `mode`가 없어 새 색인 조회에 잡히지 않고,
  보안 규칙이 수정을 막고 있어 소급 입력이 불가능함. 초기화 명령은 [배포-안내.md](배포-안내.md) 4장.

### 9. 밸런싱

- 궤도 반지름·점수표·낙하 쿨다운 조정
- 크롬북(1366×768) 화면에서 순번+이름 라벨이 읽히는지 확인 (열린 질문 2)
- 해왕성 궤도가 여러 개 쌓일 때의 처리 결정 (열린 질문 4)

### 10. 문서 갱신

- [배포-안내.md](배포-안내.md) — 두 모드 안내, `?mode=` 링크 만드는 법
- [안내문-모음.md](안내문-모음.md) — 학생 안내문에 모드 설명 추가
- `planet-merge-game/README.md` — 모드 구조 설명

### 11. 학생 파일럿

- 두 모드를 각각 해보게 한 뒤 확인 질문:
  - "수금지화목토천해를 순서대로 말해볼까요?"
  - **"목성과 해왕성 중 어느 쪽이 더 큰가요?"** (크기 오개념 확인 — concept2.md 6장)
  - "화성과 목성 사이는 왜 그렇게 멀까요?"
