# ver2 — 거리 순서 모드 (Orbit Merge) Concept

ver1(크기 순서 게임)과 **같은 사이트, 같은 조작**으로 즐기는 두 번째 모드.
같은 궤도 두 개를 합쳐 한 단계 **바깥 궤도**로 넓혀 가며, 수성 궤도에서 시작해 해왕성 궤도까지 도달한다.

- ver1 문서: [concept.md](concept.md) — 크기 순서(소행성~태양, 11단계)
- 본 문서: 거리 순서(수금지화목토천해, 8단계)

---

## 1. 왜 두 개의 모드인가

| | ver1 (크기 순서 모드) | ver2 (거리 순서 모드) |
|---|---|---|
| 배우는 것 | 천체의 **실제 지름** 순서 | 태양에서의 **거리** 순서 |
| 사다리 | 소행성 → 달 → 수성 → … → 태양 (11) | 수성 → 금성 → … → 해왕성 (8) |
| 공의 크기가 뜻하는 것 | 천체의 실제 지름 | **공전 궤도의 크기** |
| 클리어 | 태양 만들기 | 해왕성 궤도까지 넓히기 |

**두 모드를 나란히 두는 것 자체가 수업 자료다.** ver1에서 "해왕성은 목성보다 작다"를,
ver2에서 "그런데 해왕성은 목성보다 훨씬 멀다"를 배운다. 한쪽만으로는 생기기 쉬운
"큰 행성이 바깥에 있다"는 오개념을, 두 모드를 비교하면 스스로 깨뜨릴 수 있다.

교사용 발문 예시: *"목성과 해왕성 중에 누가 더 크지? 그럼 누가 더 멀지? 왜 다를까?"*

---

## 2. 학습 목표

1. 태양에서 가까운 순서 **수-금-지-화-목-토-천-해**를 순서대로 말할 수 있다.
2. 행성의 **크기 순서와 거리 순서가 다르다**는 것을 안다. (핵심 오개념 교정)
3. 화성과 목성 사이가 유난히 멀고, 바깥쪽 행성일수록 간격이 훨씬 벌어진다는 것을 느낀다.
4. (엔딩) 교과서의 태양계 그림은 좁혀 그린 것이며, 실제 거리는 훨씬 띄엄띄엄하다는 것을 안다.

목표 3·4는 게임 규칙만으로는 잘 전달되지 않는다. → 8장 **궤도 크기 설계**와 9장 **엔딩 거리자**로 따로 다룬다.

---

## 3. 천체 데이터 (8행성)

| 단계 | 행성 | 태양까지 거리 | AU | 공전 주기 | 실제 지름 |
|---|---|---|---|---|---|
| 1 | 수성 | 5,790만 km | 0.39 | 88일 | 4,879 km |
| 2 | 금성 | 1억 820만 km | 0.72 | 225일 | 12,104 km |
| 3 | 지구 | 1억 4,960만 km | 1.00 | 365일 | 12,742 km |
| 4 | 화성 | 2억 2,790만 km | 1.52 | 687일 | 6,779 km |
| 5 | 목성 | 7억 7,860만 km | 5.20 | 11.9년 | 142,984 km |
| 6 | 토성 | 14억 3,350만 km | 9.58 | 29.5년 | 116,460 km |
| 7 | 천왕성 | 28억 7,250만 km | 19.2 | 84년 | 50,724 km |
| 8 | 해왕성 | 44억 9,510만 km | 30.05 | 165년 | 49,244 km |

> **AU(천문단위)**: 태양-지구 거리(약 1억 4,960만 km)를 1로 삼은 자.
> 4학년에게는 "지구가 태양에서 떨어진 만큼을 1칸이라고 하면" 정도로 풀어 설명한다.

### 정보 카드 문구 (초안)

| 행성 | 한 줄 설명 | 알고 있나요? |
|---|---|---|
| 수성 | 태양에서 가장 가까운 행성이에요. | 태양을 한 바퀴 도는 데 88일밖에 안 걸려요. 수성의 1년은 세 달도 안 돼요. |
| 금성 | 태양에서 두 번째로 가까운 행성이에요. | 저녁 하늘과 새벽 하늘에서 가장 밝게 빛나는 별이 바로 금성이에요. |
| 지구 | 우리가 사는 행성, 태양에서 세 번째예요. | 태양빛이 지구까지 오는 데 8분 20초 걸려요. 지금 보는 햇빛은 8분 전의 빛이에요. |
| 화성 | 태양에서 네 번째, 지구 바로 바깥쪽 이웃이에요. | 지구에서 로켓을 보내면 화성까지 약 7개월이 걸려요. |
| 목성 | 태양에서 다섯 번째, 화성보다 훨씬 멀어요. | 화성과 목성 사이는 유난히 멀어요. 그 빈 곳에 작은 바위들이 모인 소행성대가 있어요. |
| 토성 | 태양에서 여섯 번째, 고리를 두른 행성이에요. | 태양을 한 바퀴 도는 데 29년이 넘어요. 토성에서 태어나면 아직 한 살도 안 됐을 거예요. |
| 천왕성 | 태양에서 일곱 번째 행성이에요. | 맨눈으로는 안 보여서, 망원경으로 처음 찾아낸 행성이에요(1781년). |
| 해왕성 | 태양에서 가장 먼 행성이에요. | 1846년에 발견됐는데, 태양을 한 바퀴 도는 데 165년이 걸려서 2011년에야 첫 바퀴를 마쳤어요. |

**오개념 방지 문구 (천왕성·해왕성 카드에 반드시 병기)**

> 궤도는 목성보다 훨씬 크지만, **행성 자체는 목성이 해왕성보다 훨씬 커요.**
> 멀리 있는 것과 큰 것은 다른 이야기예요.

---

## 4. 게임 규칙

ver1과 조작·판정이 **완전히 동일**하다. 학생이 새로 배울 것은 "무엇이 무엇으로 바뀌는가"뿐이다.

- 병 위에서 좌우로 움직여 위치를 정하고 클릭/터치로 떨어뜨린다.
- **같은 궤도 두 개가 부딪히면 한 단계 바깥 궤도가 된다.**
  수성궤도 ×2 → 금성궤도 ×2 → 지구궤도 → … → 천왕성궤도 ×2 → **해왕성궤도 = 클리어**
- 처음 나오는 궤도(수성·금성·지구)만 떨어진다. (`SPAWN_POOL_SIZE = 3`)
- 빨간 점선 위로 일정 시간 이상 넘치면 게임 오버. (ver1 판정 그대로)
- 점수: `MERGE_SCORES = [0, 10, 25, 45, 75, 120, 190, 300]`
  (index = 새로 만들어진 궤도 단계. 8단계뿐이라 ver1보다 단계당 배점을 키웠다)

### 클리어 처리

ver1은 태양끼리 부딪혀도 아무 일이 없다(미구현). ver2는 **해왕성 궤도를 만든 순간 엔딩 화면**(9장)을 띄우고,
확인을 누르면 게임은 계속 진행된다. 점수 경쟁을 위해 강제 종료하지 않는다.

---

## 5. 이 방식의 약점과 대책 ⭐

선택한 방식(수박게임 규칙 그대로)에는 **"순서를 몰라도 이길 수 있다"**는 구조적 약점이 있다.
같은 그림 둘만 찾으면 되기 때문에, 학생이 수금지화목토천해를 전혀 모른 채 클리어할 수 있다.
아래 네 장치로 메운다.

### 5-1. 순서 퀴즈 카드 (핵심 대책)

ver1의 정보 카드 자리를 그대로 쓴다. 새 궤도를 **처음** 만들면 게임이 멈추고 먼저 질문이 뜬다.

```
        ✨ 새로운 궤도 발견!

   지구 다음으로 태양에서 먼 행성은?

     [ 목성 ]   [ 화성 ]   [ 토성 ]

   (고르면 정답 확인 → 정보 카드 내용 공개)
```

- 3지선다, 보기는 매번 섞는다. **틀려도 벌점 없이** 정답을 알려주고 넘어간다(4학년 대상).
- 맞히면 보너스 점수(해당 단계 점수의 50%)와 짧은 칭찬 효과음.
- 한 번 본 궤도는 다시 묻지 않는다. (ver1의 `seenStages` 로직 그대로)
- `config.js`의 `ORDER_QUIZ = true`로 켜고 끌 수 있다. (시간이 없는 차시에는 꺼서 운영)

> 이 장치가 있어야 "순서를 모르면 진행이 매끄럽지 않다"가 성립한다. **ver2에서 가장 중요한 교육 장치다.**

### 5-2. 공에 순번을 함께 표시

떨어지는 궤도마다 `③ 지구`처럼 **순번과 이름**을 붙인다. 게임 내내 눈에 들어와 순서가 자연히 각인된다.

### 5-3. 거리 순서표 패널

ver1의 "크기 순서표"를 그대로 재사용하되 내용을 거리 순으로 바꾸고, 상단 안내 문구를 **거울처럼 대응**시킨다.

| ver1 | ver2 |
|---|---|
| "실제 지름이 작은 것부터 큰 순서예요. 태양에서 가까운 순서가 아니에요!" | "**태양에서 가까운 순서**예요. 공의 크기는 **공전 궤도**의 크기이고, **행성 자체의 크기가 아니에요!**" |

### 5-4. 엔딩 거리자

9장 참조. 순서를 다 외운 뒤 "그런데 실제 간격은 이렇다"로 마무리한다.

---

## 6. 남은 위험: 크기 오개념

ver1에서 "합치면 커진다 = 실제 크기"를 몸으로 익힌 학생이, ver2에서 지구 다음에 화성이 더 크게 나오는 것을 보면
**"화성이 지구보다 크다"**고 오해할 수 있다. 이것이 이 방식의 가장 큰 위험이며, 다음 세 가지로 막는다.

1. **공을 행성이 아니라 궤도로 그린다.** (7장) 공 = 고리, 그 위에 작은 행성 하나.
2. **행성 점의 크기는 실제 지름 비율을 따른다.** 고리는 커지는데 그 위의 목성 점이 해왕성 점보다 크다 →
   화면 하나로 "궤도 크기 ≠ 행성 크기"가 눈에 보인다.
3. **문구로 못 박는다.** 순서표 상단(5-3), 천왕성·해왕성 정보 카드(3장), 모드 선택 화면에 모두 명시.

> **파일럿 때 반드시 확인할 것**: 게임 후 학생에게 "목성과 해왕성 중 뭐가 더 커?"를 물어본다.
> 여기서 해왕성이라고 답하는 학생이 많으면 위 장치가 부족한 것이므로, 공 위에 실제 지름 막대를
> 함께 그리는 등 보강이 필요하다.

---

## 7. 비주얼 — "궤도 고리" 스프라이트

ver1의 SVG 코드 생성 방식(이미지 파일 없음, 저작권 무관)을 그대로 쓴다.

```
   공 하나 = 하나의 궤도

        ╭───────────────╮
       ╱                 ╲
      │        ✦          │   ← 중심의 작은 표식(태양 자리)
      │                   │
       ╲               ●  ╱   ← 고리 위에 올라탄 행성
        ╰───────────────╯      (점 크기 = 실제 지름 비율)
             ③ 지구            ← 순번 + 이름
```

- **고리**: 각 행성 색의 얇은 원, 안쪽은 아주 옅게 채워 물리적으로 "꽉 찬 공"임을 알 수 있게 한다.
- **행성 점**: ver1의 행성 스프라이트를 축소해 재사용. 표정도 유지(귀여운 카툰 톤 일관성).
- **행성 점 크기**: 실제 지름을 압축해 매핑 — 수성 ≈ 5px … 목성 ≈ 14px. 목성이 항상 가장 크게 보인다.
- **순번+이름 라벨**: 고리 아래쪽에 배치. 작은 화면에서는 순번만 표시.
- 병합 시 파티클, 표정 변화, 효과음은 ver1과 동일하게 재사용.

배경에는 병 바깥 좌상단에 태양을 하나 그려 "이 궤도들은 저 태양을 도는 길"이라는 맥락을 준다.

> **선행 검증 필요**: 고리 안쪽이 비어 보이는데 물리적으로는 충돌하므로 어색할 수 있다.
> 스프라이트를 만든 직후 실제 화면에서 확인하고, 어색하면 안쪽 채움을 더 진하게 조정한다.

---

## 8. 궤도 크기(반지름) 설계

공의 크기를 실제 궤도 비율에 얼마나 충실하게 할지는 **학습 정확성 ↔ 게임 재미**의 맞교환이다.

| 단계 | 행성 | AU | (A) 실제 비율 | (B) 균등 계단 | **(C) 절충 — 채택** |
|---|---|---|---|---|---|
| 1 | 수성 | 0.39 | 22 | 22 | **22** |
| 2 | 금성 | 0.72 | 27 | 27 | **27** |
| 3 | 지구 | 1.00 | 30 | 34 | **32** |
| 4 | 화성 | 1.52 | 35 | 42 | **39** |
| 5 | 목성 | 5.20 | 53 | 52 | **53** |
| 6 | 토성 | 9.58 | 65 | 64 | **65** |
| 7 | 천왕성 | 19.2 | 82 | 79 | **81** |
| 8 | 해왕성 | 30.05 | 97 | 97 | **97** |

- (A)는 `반지름 ∝ AU^0.341`로 압축한 값. 금성·지구가 27↔30으로 너무 비슷해 구분이 어렵다.
- (B)는 매 단계 같은 비율로 커지는 값. 구분은 쉽지만 거리 정보가 사라진다.
- **(C) 절충안 채택**: 구분은 충분히 되면서, **화성(39) → 목성(53) 구간만 유난히 크게 벌어진다.**
  게임 화면의 생김새 자체가 "화성과 목성 사이가 멀다"를 말해 주고, 목성 카드의 소행성대 이야기로 이어진다.

세 벌의 숫자를 모두 `objects.js` 주석에 남겨 두어, 파일럿 결과에 따라 교사가 바꿔 끼울 수 있게 한다.

---

## 9. 엔딩 — 실제 거리자

해왕성 궤도를 완성하면(또는 아무 때나 "실제 거리 보기" 버튼으로) 전체 화면으로 띄운다.

```
☀수금지화              목성            토성                 천왕성                    해왕성
├┼┼┼┼──────────────────┼───────────────┼────────────────────┼─────────────────────────┤
0                                                                                    45억 km

  수성·금성·지구·화성은 전부 왼쪽 끝 5% 안에 몰려 있어요.
  교과서 그림은 좁혀 그린 거예요. 실제로는 이만큼 띄엄띄엄합니다.

  ☀ → 지구   빛으로 8분 20초
  ☀ → 해왕성 빛으로 4시간 10분
```

- 실제 비율 그대로 배치한다. 안쪽 네 행성이 뭉개져 보이는 것이 **바로 이 화면의 요점**이다.
- 확대 토글: 안쪽 4행성만 따로 확대해 보여주는 버튼.
- 보너스 카드: **"명왕성은 왜 빠졌을까?"** — 1930년 발견되어 아홉 번째 행성이었지만,
  2006년에 행성의 조건이 정리되면서 왜소행성으로 분류가 바뀌었다.
- 수업 정리용으로 교사가 언제든 띄울 수 있도록 게임 화면에 상시 버튼을 둔다.

---

## 10. 기술 설계 — ver1 재사용 범위

병합 규칙이 `mergeLogic.js`로 이미 분리되어 있어 구조 변경이 크지 않다.

### 그대로 쓰는 것 (수정 없음)

물리 엔진 · 낙하/조준 조작 · 게임오버 판정 · 표정 시스템 · 파티클 · 사운드 합성 ·
별명 생성기 · 학급 코드 · 순위표 UI · 정보 카드 일시정지 구조 · 반응형 병 크기 · 배포 파이프라인

### 새로 만드는 것

| 파일 | 내용 |
|---|---|
| `src/game/modes/sizeMode.js` | ver1 데이터를 모드 객체로 감싼 것 |
| `src/game/modes/distanceMode.js` | ver2 데이터 (8행성, 궤도 반지름, 점수표, 안내 문구) |
| `src/game/orbitSprites.js` | 궤도 고리 SVG 생성기 |
| `src/components/OrderQuiz.jsx` | 순서 퀴즈 카드 (5-1) |
| `src/components/DistanceRuler.jsx` | 엔딩 거리자 (9장) |
| `src/components/ModeSelect.jsx` | 첫 화면 모드 선택 |

### 손봐야 하는 것

- `objects.js` → 모드가 주는 데이터를 쓰도록 변경 (`STAGES` 직접 import 제거)
- `engine.js` → `createGame(el, { mode, ... })`로 모드를 주입받음
- `PlanetGuide.jsx` → 제목·안내 문구·목록을 모드에서 받아오도록
- `config.js` → `ORDER_QUIZ`, `DEFAULT_MODE` 추가

### 모드 선택 방식

- 첫 접속 시 모드 선택 화면 → 고른 모드를 기억(별명과 같은 저장 규칙: 탭을 닫으면 초기화)
- URL로 바로 지정: `?mode=distance` — 선생님이 수업에서 한쪽만 열게 할 때 사용
- 학급 코드와 함께 쓸 수 있음: `?room=haneul-4-2&mode=distance`
- 게임 화면 상단에 모드 전환 버튼

### 순위표 — 주의 ⚠️

두 모드의 점수를 한 순위표에 섞으면 안 된다. 기록에 `mode` 필드를 추가하고,
Firestore 색인 3개를 `(room, mode, 기간키, score desc)`로 바꾸고, 보안 규칙에 `mode` 허용값을 추가해야 한다.

**기존 기록에는 `mode` 필드가 없어 새 색인 조회에 잡히지 않는다.** 보안 규칙이 수정을 막고 있어
소급 입력도 불가능하다. → **ver2 배포 시점에 순위표를 한 번 초기화**하는 것을 전제로 한다.
(새 학기·새 대회 시작과 맞물리면 자연스럽다. 초기화 명령은 [배포-안내.md](배포-안내.md) 4장에 있다.)

---

## 11. 구현 순서

각 단계가 끝날 때마다 화면에서 확인할 수 있는 단위로 끊었다.

1. **모드 구조 만들기** — `modes/` 도입, ver1을 `sizeMode`로 감싸기.
   *ver1이 이전과 똑같이 동작하는지 확인하는 것이 이 단계의 전부다.*
2. **거리 모드 데이터** — `distanceMode.js`에 8행성 데이터·반지름(8장 C안)·점수표 입력
3. **궤도 고리 스프라이트** — 고리 + 행성 점 + 순번 라벨 SVG 생성, 8단계 미리보기로 눈으로 검증
4. **거리 모드 플레이 가능** — 엔진에 모드 주입, 순서표 패널 교체 → 처음부터 끝까지 한 판 진행
5. **순서 퀴즈 카드** — 3지선다, 보너스 점수, 한 번 본 것 제외, `ORDER_QUIZ` 토글
6. **엔딩 거리자** — 실제 비율 배치, 확대 토글, 명왕성 카드, 상시 버튼
7. **모드 선택 화면 + URL 파라미터**
8. **순위표 mode 분리** — 필드·색인·보안 규칙, 순위표 초기화
9. **밸런싱** — 반지름·점수·낙하 쿨다운 조정, 크롬북 화면에서 라벨 가독성 확인
10. **문서 갱신** — 배포-안내.md, 안내문-모음.md, README에 두 모드 안내 추가
11. **학생 파일럿** — 6장의 "목성 vs 해왕성" 확인 질문 포함

---

## 12. 확장 아이디어 (당장은 하지 않음)

- **거리 순서 스피드런 모드**: 순서표를 감춘 채 8행성을 순서대로 배치하는 30초 도전
- **두 모드 통합 점수**: 크기 순서와 거리 순서를 모두 클리어한 학생에게 배지
- **공전 주기 체험**: 완성된 궤도들이 실제 주기 비율로 도는 애니메이션
  (수성이 여덟 바퀴 도는 동안 해왕성은 거의 안 움직인다)
- **소행성대 장애물**: 화성 궤도와 목성 궤도 사이에 작은 바위가 섞여 떨어지는 난이도 옵션

---

## 13. 열려 있는 질문

문서 작성 시점에 확정하지 못한 것들. 구현 중 판단이 필요하다.

1. ~~**고리 안쪽 채움 정도**~~ — 해결됨. 안쪽을 어두운 우주로 채우고 그 위에 얼굴을 그려서,
   비어 보이지 않으면서 "행성이 이만큼 크다"로도 오해되지 않게 했습니다.
2. ~~**작은 화면에서 순번+이름 라벨 가독성**~~ — 해결됨. 물리 좌표가 아니라 **화면에 찍히는 크기**로
   글자를 정하도록 바꿔, 병이 아무리 작아져도 10~14px 로 보입니다. (크롬북 8.3px → 10px)
3. ~~**순서 퀴즈의 난이도**~~ — 해결됨. **순서상 가장 가까운 행성 2개**를 보기로 씁니다.
   멀리 떨어진 행성을 주면 순서를 몰라도 찍어서 맞힐 수 있어 학습 효과가 없기 때문입니다.
   파일럿에서 너무 어려우면 `distanceMode.jsx`의 `quiz`에서 정렬 기준만 바꾸면 됩니다.
4. ~~**클리어 후 계속 진행**~~ — 해결됨. 해왕성 궤도 두 개가 만나면 "태양계 하나 완성"으로
   사라지며 +500점. 병이 막히지 않아 완성 뒤에도 계속 즐길 수 있습니다.
