# 온라인 순위표 켜기 (Firebase 설정 안내)

지금 게임의 순위표는 **"이 기기에만 저장되는 연습용"**으로 동작합니다.
아래 순서대로 하면 **여러 반이 함께 보는 온라인 순위표**로 바뀝니다. 코딩 지식은 필요 없고, 15~20분 정도 걸립니다.

> 설정하지 않아도 게임은 정상 작동합니다. 학급 하나에서만 쓰신다면 지금 상태로도 충분합니다.

---

## 1단계. Firebase 프로젝트 만들기 (무료)

1. https://console.firebase.google.com 에 학교 구글 계정으로 로그인
2. **프로젝트 만들기** 클릭 → 이름은 `planet-merge-game` 처럼 영어로 입력
3. "이 프로젝트에 Google 애널리틱스 사용" → **끄기**(사용 안 함) 선택 후 만들기
4. 요금제는 **Spark(무료)** 그대로 두세요. 학교 규모에서는 무료 한도로 충분합니다.

## 2단계. 데이터베이스 만들기

1. 왼쪽 메뉴에서 **빌드 → Firestore Database** 클릭
2. **데이터베이스 만들기** 클릭
3. 위치는 **asia-northeast3 (서울)** 선택 (한국에서 가장 빠릅니다)
4. 시작 모드는 **테스트 모드**를 선택 (4단계에서 반드시 안전하게 바꿉니다)

## 3단계. 설정 키 받아서 붙여넣기

1. 왼쪽 위 **톱니바퀴 → 프로젝트 설정** 클릭
2. 아래로 내려 "내 앱"에서 **웹(`</>`) 아이콘** 클릭
3. 앱 닉네임은 아무거나(`planet-merge-game`) 입력하고 **앱 등록**
4. 화면에 아래 같은 코드가 나옵니다.

```js
const firebaseConfig = {
  apiKey: "AIzaSy...",
  authDomain: "planet-merge-game.firebaseapp.com",
  projectId: "planet-merge-game",
  storageBucket: "planet-merge-game.firebasestorage.app",
  messagingSenderId: "123456789",
  appId: "1:123456789:web:abcdef"
};
```

5. `planet-merge-game/src/game/leaderboardConfig.js` 파일을 메모장으로 열어
   맨 위 `FIREBASE_CONFIG`의 빈 따옴표 안에 위 값들을 그대로 옮겨 적고 저장하세요.
6. GitHub Desktop에서 **Commit → Push** 하면 1~2분 뒤 온라인 순위표가 켜집니다.

> **키를 공개해도 되나요?** 네, 이 값들은 웹앱에 공개되는 것이 정상입니다.
> 실제 보안은 다음 단계의 "보안 규칙"이 담당합니다.

## 4단계. 보안 규칙 적용 (⚠️ 꼭 하세요)

테스트 모드를 그대로 두면 **누구나 기록을 지우거나 마음대로 쓸 수 있고, 30일 뒤 차단**됩니다.

1. Firebase 콘솔 → **Firestore Database → 규칙** 탭
2. 기존 내용을 모두 지우고, 이 저장소의 [`firestore.rules`](firestore.rules) 파일 내용을 복사해 붙여넣기
3. **게시** 클릭

이 규칙이 하는 일:
- 순위표 읽기는 누구나 가능(로그인 없이 링크로 접속하므로)
- 기록 추가만 가능하고, **수정·삭제는 불가능**(장난으로 남의 기록을 지울 수 없음)
- 별명 8자 이하, 점수 0~100,000 범위만 허용(비정상 점수 차단)
- 시간은 서버 시각만 인정(날짜 조작 방지)

## 5단계. (선택) 별명 목록 손보기

별명은 `planet-merge-game/src/game/nicknames.js` 의 두 목록을 조합해 만들어집니다.
학생들이 좋아할 낱말을 더 넣고 싶으면 목록에 추가하세요 (각 낱말 4글자 이하 권장).

```js
export const ADJECTIVES = ['반짝이는', '빛나는', ...]   // 꾸밈말
export const NOUNS = ['소행성', '달빛', '토성', ...]     // 우주 낱말
```

---

## 자주 묻는 것

**Q. 순위표를 새 학기에 초기화하려면?**
Firebase 콘솔 → Firestore Database → `scores` 컬렉션 → 오른쪽 점 3개 메뉴 → **컬렉션 삭제**.
지운 뒤에는 학생 화면을 새로고침하면 빈 순위표가 됩니다.

**Q. 무료 한도를 넘지 않을까요?**
무료 Spark 플랜은 하루 읽기 5만 회, 쓰기 2만 회입니다.
한 학생이 한 판에 쓰기 1회, 순위표 볼 때 읽기 몇 회 수준이라 전교생이 써도 여유롭습니다.

**Q. 인터넷이 끊기면요?**
게임은 그대로 진행되고, 점수는 기기에 저장해 두었다가 연결되면 자동으로 전송합니다.
학생 화면에는 "연결되면 자동으로 올릴게요"라고 안내됩니다.

**Q. 개인정보가 걱정됩니다.**
저장하는 것은 **별명·점수·도달 천체·시각** 네 가지뿐입니다. 이름·학번·반은 입력받지도, 저장하지도 않습니다.
보안 규칙에서도 이 네 가지 외의 정보가 들어오면 거부합니다.

**Q. 학생이 이상한 별명을 쓰면 어떡하죠?**
별명은 직접 입력하는 것이 아니라, 미리 준비된 "꾸밈말 + 우주 낱말" 조합(예: 반짝이는토성) 중에서 **고르는 방식**입니다.
그래서 부적절한 말이 올라올 수 없습니다. 개발자도구로 임의의 값을 넣으려 해도 서버 규칙에서 거부됩니다.
