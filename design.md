# 행성 합치기 게임 — Design Reference (from playsuikagame.com/ko)

> ⚠️ **확인 범위 안내**
> 이 문서의 "1. 레이아웃/콘텐츠 구조"는 playsuikagame.com/ko 페이지의 텍스트 콘텐츠를 직접 확인해 작성했습니다.
> 반면 "2~4. 색상 / 폰트 / 여백"은 이 환경에서 해당 사이트로의 브라우저 직접 접속이 차단되어 **실제 CSS 값을 측정하지 못했고**, 수박게임(Suika Game) 계열 클론들의 통상적인 디자인 관례를 기반으로 한 **제안값**입니다. 스크린샷이나 개발자도구(F12 → Elements → Computed) 캡처를 공유해주시면 실제 값으로 정확히 교체하겠습니다.

---

## 1. 레이아웃 / 콘텐츠 구조 (실제 확인됨)

### 게임 화면 영역
- **상단 스코어보드**: "스코어" 라벨 + 브랜드명("bubble") + 현재 점수 + 최고 점수
- **다음 오브젝트 미리보기**: "다음"이라는 라벨과 함께 다음에 낙하할 오브젝트 표시
- **게임 컨테이너**: 오브젝트가 쌓이는 병 형태 낙하 공간 (원본 수박게임 구조 유지)
- **게임오버 후 결과 표시**: "당신의 점수" / "최고의 점수" 재노출
- **숨겨진 요소**: "신카의 고리"라는 이스터에그성 텍스트 발견 — 특정 조건에서 노출되는 히든 기능으로 추정

### 리더보드 패널
- 탭 구조: **매일 / 매주 / 매월** 3개 필터
- 각 탭마다 **순위 1~5위 리스트** (순위, 점수) 노출
- 실시간/누적 집계로 추정되는 큰 점수값(예: 1000765) 확인됨

### 콘텐츠(설명) 섹션 — 게임 화면 하단 스크롤 영역
페이지 하단에 SEO/안내 목적의 텍스트 콘텐츠가 이어지는 랜딩페이지형 구조:
1. "수박 게임이란 무엇인가요?" — 게임 소개
2. "수이카 게임을 하는 방법은?" — 플레이 방법
3. "수이카 게임을 효과적으로 플레이하는 팁" — 불릿 3개(미리 계획하기 / 간격 피하기 / 정기적인 연습)
4. "수이카 게임 & 친구들" — 커뮤니티/리더보드 관련 설명
5. "영어 및 일본어 버전" — 다국어 지원 안내

→ **구조 요약**: 상단 "게임 영역"(고정형 UI) + 하단 "설명 콘텐츠 영역"(스크롤형 롱폼 텍스트)의 2단 구성.

---

## 2. 색상 가이드 (제안값 — 실제 값 미검증)

| 역할 | 제안 색상 | 비고 |
|---|---|---|
| 배경(전체) | 다크 네이비 `#1A1F2E` 또는 라이트 크림 `#FFF8ED` 중 선택 | 수박게임 클론은 크게 "다크 테마"와 "파스텔 라이트 테마" 두 갈래로 갈림 |
| 게임 컨테이너(병) | 반투명 화이트 `rgba(255,255,255,0.08)` + 옅은 보더 | 내부 오브젝트가 잘 보이도록 대비 확보 |
| 강조/CTA 버튼 | 오렌지~옐로우 계열 `#FFB800` | 과일(수박) 테마 특유의 따뜻한 포인트 컬러 |
| 텍스트 기본 | 다크 배경 시 `#F5F5F5`, 라이트 배경 시 `#222222` | |
| 리더보드 순위 강조(1위) | 골드 `#FFD54F` | 게이미피케이션 관례 |

**행성 게임 적용 제안**: 우주 테마이므로 다크 네이비/블랙 배경 쪽이 자연스럽고, 강조색은 오렌지 대신 별빛을 연상시키는 옐로우~화이트 계열을 추천합니다.

---

## 3. 폰트 가이드 (제안값 — 실제 값 미검증)

- 한글 지원이 필수이므로 웹폰트는 **Pretendard** 또는 **Noto Sans KR** 계열이 일반적 (가독성+무료 라이선스)
- 게임 특유의 통통 튀는 느낌을 원하면 굵고 둥근 헤드라인 폰트(예: **BM둥근모**, **카페24 아네모네**, **여기어때 잘난체**) 조합 고려 — 헤드라인/점수 표기에만 부분 사용 권장
- 점수·숫자류는 **Bold~ExtraBold** 굵기로 강조하는 것이 일반적 패턴
- 본문/설명 텍스트는 Regular~Medium 굵기로 대비

---

## 4. 여백 / 레이아웃 규칙 (제안값 — 실제 값 미검증)

- 카드형 UI(리더보드, 설명 섹션 블록)는 **라운드 코너 8~16px** + 옅은 그림자(`box-shadow: 0 2px 8px rgba(0,0,0,0.15)`)
- 섹션 간 수직 여백은 넉넉하게 **48~80px** (스크롤형 랜딩페이지 특성상)
- 게임 컨테이너는 **화면 중앙 정렬**, 좌우 대칭 여백
- 상단 스코어보드 요소들은 **가로 flex 배치**, 요소 간 12~24px 간격 추정
- 전체적으로 모바일 반응형을 고려한 **max-width 컨테이너(예: 480~600px)** 구조로 추정 (게임 자체가 세로형 병 형태이기 때문)

---

## 5. 정확도를 높이기 위한 다음 단계 (선택)

- [ ] 실제 화면 스크린샷 공유 → 시각적으로 색상/레이아웃 재검토
- [ ] 브라우저 개발자도구(F12) → Elements 탭에서 body/컨테이너 `computed style` 캡처해서 공유 → 정확한 HEX/폰트/px 값으로 본 문서 업데이트
- [ ] 원하시면 위 "제안값" 그대로 먼저 프로토타입에 적용하고, 추후 실측값으로 교체하는 방식으로 진행 가능
