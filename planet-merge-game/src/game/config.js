// ---------------------------------------------------------------
// 게임 밸런싱 설정 — 숫자만 바꾸면 게임 느낌이 바뀝니다 (코드 수정 불필요)
// ---------------------------------------------------------------

// [병(컨테이너) 크기] px. 세로가 길수록 게임이 길어지고, 가로가 넓을수록 쉬워짐
export const CONTAINER_WIDTH = 400
export const CONTAINER_HEIGHT = 600

// [천체 크기 배율] 1 = objects.js 의 radius 그대로. 0.9 로 하면 전체가 10% 작아져 더 오래 버팀
export const SIZE_SCALE = 1

// [낙하 시작 높이] 병 맨 위에서 몇 px 아래에서 떨어뜨릴지
export const SPAWN_Y = 44

// [게임오버 라인] 병 맨 위에서 몇 px 아래인지
export const GAME_OVER_LINE_Y = 120
// 천체가 라인 위로 이 비율 이상 올라오면 "위험" 상태
//   0.5 = 절반(중심)이 라인 위 / 0.3 = 조금만 걸쳐도 위험(더 어려움) / 0.8 = 거의 다 올라와야 함(더 쉬움)
export const GAME_OVER_OVERLAP = 0.5
// 위험 상태가 이만큼(ms) 이어지면 게임오버. 짧을수록 엄격함
export const GAME_OVER_HOLD_MS = 1000
// 방금 떨어뜨린 천체가 라인을 통과하는 동안은 판정에서 제외 (ms)
export const SPAWN_GRACE_MS = 1200

// [연속 낙하 제한] 한 번 떨어뜨린 뒤 다음 낙하까지 기다리는 시간(ms)
export const DROP_COOLDOWN_MS = 450

// [물리]
export const GRAVITY_Y = 1 // 중력 세기 (1 = 기본, 1.3 = 더 빨리 떨어짐)
export const RESTITUTION = 0.12 // 튕김 (0 = 안 튕김, 0.3 = 통통 튐)
export const FRICTION = 0.15 // 표면 마찰 (높을수록 미끄러지지 않고 잘 쌓임)
export const FRICTION_STATIC = 0.5 // 멈춰 있을 때 버티는 힘
export const DENSITY = 0.0012 // 무게감 (큰 천체가 작은 천체를 얼마나 밀어내는지에 영향)

// [모드별 설정] ------------------------------------------------
// 게임 모드마다 다른 값입니다. 모드가 늘어나면 여기에 항목을 추가합니다.
//   spawnPoolSize : 처음 몇 단계까지만 랜덤으로 떨어질지 (5 = 소행성~금성)
//   mergeScores   : 병합 결과 단계에 따른 점수. 큰 천체일수록 많이 줌
export const MODE_TUNING = {
  size: {
    spawnPoolSize: 5,
    mergeScores: [0, 10, 20, 35, 55, 80, 110, 150, 200, 270, 400],
  },
  // 거리 순서 게임 — 8단계뿐이라 단계당 배점을 크기 순서 게임보다 크게 잡았습니다
  distance: {
    spawnPoolSize: 3, // 수성·금성·지구 궤도만 떨어짐
    mergeScores: [0, 10, 25, 45, 75, 120, 190, 300],
    // 마지막 단계(해왕성 궤도) 두 개가 만나면 "태양계 하나 완성"으로 사라지며 받는 점수.
    // 이렇게 하지 않으면 가장 큰 궤도들이 병 바닥에 쌓여 게임이 금방 끝납니다.
    finalPairScore: 500,
  },
}

// [표정] ms
export const SURPRISED_MS = 700
export const MERGE_HAPPY_MS = 1000

// ---------------------------------------------------------------
// [교실 사용 설정]
// ---------------------------------------------------------------
// 탭(또는 브라우저)을 닫으면 개인 기록을 초기화할지 여부
//   true  (기본) = 최고 점수·별명·본 정보 카드가 탭을 닫으면 사라짐
//                  → 학교 공용 기기에서 다음 학생이 깨끗한 상태로 시작
//   false        = 같은 기기에서 계속 이어짐 (개인 기기에서 기록을 쌓고 싶을 때)
//
// ※ 새로고침(F5)이나 다시 시작은 초기화되지 않고, 탭을 닫아야 초기화됩니다.
// ※ 순위표에 올라간 기록과 학급 코드는 이 설정과 무관하게 그대로 유지됩니다.
export const RESET_ON_TAB_CLOSE = true

// [점검 중 잠그기]
//   true  = 두 게임 모두 "공사 중"으로 잠기고 아무도 플레이할 수 없습니다.
//           순위표를 초기화하거나 새 버전을 준비하는 동안 학생이 들어와도
//           안내 문구만 보이고 기록이 섞이지 않습니다.
//   false = 평소대로 두 게임 모두 열림
//
// ※ 여는 방법: 이 값을 false 로 바꾸고 다시 배포하면 됩니다.
export const MAINTENANCE = true

// 점검 중일 때 학생 화면에 보여줄 안내 (MAINTENANCE 가 true 일 때만 쓰임)
export const MAINTENANCE_MESSAGE = {
  title: '잠시 준비 중이에요',
  body: '새 게임을 만들고 있어요. 조금만 기다려 주세요!',
}

// [순서 퀴즈] 거리 순서 게임에서만 씁니다.
//   true  (기본) = 새 궤도를 처음 만들면 정보 카드 앞에 순서를 묻는 3지선다가 나옴
//                  → 순서를 모르면 그냥 지나칠 수 없게 하는 장치
//   false        = 퀴즈 없이 정보 카드만 나옴 (시간이 빠듯한 차시에)
//
// ※ 틀려도 벌점은 없고 정답을 알려준 뒤 넘어갑니다. 맞히면 보너스 점수를 줍니다.
export const ORDER_QUIZ = true
