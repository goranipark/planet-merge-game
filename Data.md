# Data.md — 온라인 리더보드 데이터 계층 To-Do

여러 반이 함께 플레이하는 것이 확정되어, ToDo.md 2장에서 "MVP는 서버/DB 불필요"로 보류했던 온라인 리더보드를 지금 도입합니다.

---

## 1. 백엔드 선택: Firebase Firestore 추천

| 기준 | Firebase Firestore | Google Sheets + Apps Script |
|---|---|---|
| 여러 반 동시 제출 처리 | 안정적 (분산 DB) | 동시 쓰기 많으면 지연/오류 가능성 |
| 일간/주간/월간 집계 | 쿼리로 간단히 처리 | 날짜 필터 로직 직접 구현 필요 |
| 실시간 갱신 | 지원(onSnapshot) | 미지원(새로고침 필요) |
| 기존 익숙도 | 신규 학습 필요 | 이미 익숙함(GAS) |
| 비용 | 무료 Spark 플랜으로 충분 | 완전 무료 |

**결정**: 여러 반 동시 접속·실시간성을 고려해 **Firebase Firestore**로 진행합니다. (GAS+Sheets에 더 익숙하셔서 그쪽을 원하시면 6장 구조를 Apps Script Web App으로 그대로 치환 가능합니다.)

---

## 2. 데이터 모델 설계

```
scores (컬렉션)
 └─ {문서ID} (자동 생성)
     - nickname: string       // 미리 준비된 조합에서 고른 별명 (아래 4장 참고)
     - score: number
     - stageReached: string   // 도달한 최고 천체, 예: "목성"
     - createdAt: timestamp
```

> **변경**: 처음에는 `className`(반)도 저장할 계획이었으나, 개인정보를 최소한으로 모으기 위해
> **반 정보는 아예 수집하지 않기로** 했습니다. 보안 규칙에서도 이 4개 외의 필드는 거부합니다.

- 별도 집계 테이블 없이 `createdAt` 범위 필터(일/주/월)로 조회 → 학교 규모 트래픽에서는 이 방식으로 충분

---

## 3. To-Do: Firebase 프로젝트 설정

> **진행 상태**: 코드 쪽 준비는 모두 끝났습니다. 아래 콘솔 작업만 하면 온라인으로 전환됩니다.
> 단계별 안내는 [FIREBASE-설정안내.md](FIREBASE-설정안내.md) 참고.
> 설정 전까지는 순위표가 "이 기기에만 저장되는 연습용"으로 동작합니다.

- [ ] **(선생님)** Firebase 콘솔에서 프로젝트 생성 (무료 Spark 플랜)
- [ ] **(선생님)** Firestore Database 활성화 — 위치는 asia-northeast3(서울)
- [ ] **(선생님)** 웹 앱 등록 → SDK config 키를 `src/game/leaderboardConfig.js`에 붙여넣기
- [x] React 프로젝트에 설치: `npm install firebase`
- [x] 초기화 코드 작성 → `src/game/leaderboard.js` (동적 import로 분리해, 설정 전에는 학생 기기가 firebase 코드를 내려받지 않음)
- [x] Firestore Security Rules 작성 → [`firestore.rules`](firestore.rules) — **(선생님)** 콘솔 규칙 탭에 붙여넣고 게시 필요

---

## 4. To-Do: 개인정보/안전 체크리스트 (초등 대상 — 반드시 확인)

- [x] **반 정보 수집 안 함** — 저장하는 개인 관련 정보는 별명 하나뿐
- [x] 별명은 **직접 입력이 아니라 고르기** 방식 — "꾸밈말 + 우주 낱말"(예: 반짝이는토성) 조합 중에서 선택하므로
      부적절한 말이 올라올 수 없음. 개발자도구로 임의 값을 넣어도 서버 규칙과 클라이언트 양쪽에서 거부됨
- [x] 자유 입력이 꼭 필요하면 `leaderboardConfig.js`의 `ALLOW_CUSTOM_NICKNAME = true`로 전환 가능(권장하지 않음)
- [ ] **(선생님)** 개인정보 활용 관련 학교 규정·가정통신문 필요 여부 확인 (별명만 쓰므로 대부분 불필요하지만 학교 방침 우선 확인)

---

## 5. To-Do: 부정 점수 방지

클라이언트에서 직접 Firestore에 쓰는 구조는 개발자도구로 점수 조작이 가능합니다. 완벽 차단은 어렵지만:

- [x] Firestore Security Rules에 점수 범위(0~100,000) 초과 시 쓰기 거부 조건 추가 — 클라이언트에서도 동일하게 검증
- [x] 규칙에서 **수정·삭제 전면 차단**(장난으로 남의 기록을 지울 수 없음), 시각은 서버 시각만 인정
- [ ] (여유 있으면) Cloud Functions로 점수 제출을 검증 후 기록 — 학교 규모에서는 필수는 아님

---

## 6. To-Do: React 연동

- [x] `submitScore({nickname, score, stageReached})` 작성 — 게임오버 시 자동 호출
- [x] 기간별 조회 함수 — 매일/매주/매월 날짜 필터 + 점수 내림차순 상위 5위
- [x] `LeaderboardPanel.jsx` — 탭 UI(매일/매주/매월) + 순위 리스트 + 메달 표시 + 새로고침 버튼
- [x] `NicknamePicker.jsx` — 안전한 별명 6개 중 고르기 + "다른 별명 보기", 기기에 저장되어 매번 고를 필요 없음("바꾸기"로 변경 가능)
- [x] 오프라인 대비: 제출 실패 시 로컬 큐에 저장 → 다음 접속 때 자동 재전송, 화면에 "보내지 못한 기록 N개" 표시

---

## 7. To-Do: 테스트

- [x] 30건 동시 제출 시뮬레이션 — 유실 없이 전부 저장, 순위 정렬 정확
- [x] 이상값 방어 테스트 — 빈 별명, 임의 별명(실명·비속어 포함), 음수 점수, 과대 점수, 숫자가 아닌 점수 모두 차단 확인
- [x] 반 정보를 몰래 끼워 넣어도 저장되지 않는지 확인
- [x] 전체 흐름 테스트 — 별명 고르기 → 플레이 → 게임오버 → 자동 등록 → 순위표 즉시 반영
- [x] 무료 할당량 확인 — Spark 플랜 하루 읽기 5만/쓰기 2만. 한 판당 쓰기 1회 수준이라 전교생 사용에도 여유
- [ ] **(선생님)** Firebase 설정 후 실제 기기 2대 이상으로 동시 접속 확인

---

## 8. To-Do: 배포 전 최종 점검

- [ ] **(선생님)** Security Rules를 프로덕션 기준으로 전환했는지 확인 (테스트 모드 방치 금지 — 30일 뒤 차단됨)
- [x] 리더보드 초기화 방법 정리 → [FIREBASE-설정안내.md](FIREBASE-설정안내.md) "자주 묻는 것" 참고 (콘솔에서 `scores` 컬렉션 삭제)
- [x] GitHub Pages 배포 상태에서 순위표 UI·저장·조회 정상 동작 확인 (연습용 모드 기준)
- [ ] **(선생님)** Firebase 설정 후 배포본에서 온라인 저장이 되는지 최종 확인
